# Change Log

Append-only. Newest entries at the bottom. Local counterpart to git history.

| Timestamp | Change |
| --- | --- |
| 2026-09-17T14:20:17-04:00 | Project created: technical concept (five surfaces, scanner/catalogue/scoring, preview compositor, transactional apply, polkit helper, GTK3→GTK4 rules), comprehensive README with the GTK Python stack, LICENSE + NOTICE, git remote MensuraMedia/lin-wallpapers. Design phase; no application code. |
| 2026-09-18T09:50:50-04:00 | Doc consistency: README roadmap M5 and packaging row no longer mention a daemon or service units; LICENSE disclaimer now describes this project (boot configuration, not thermal settings); route list reconciled (eight sidebar routes + about, diagnostics from M6) in concept §13 and milestones M0.4; reference README points at `preview/surfaces/splash.py`; `pkexec` added to runtime packages (separate package on Debian 12+/Ubuntu 24.04). |
| 2026-09-18T09:50:50-04:00 | M0 started: package `src` (app ID io.mensuramedia.LinWallpapers, v0.0.1); `gtk_version.py` single GTK gate; `ui/compat.py`; shell derived from gtk-python-dashboard-starter ported to `Gtk.Application` (single instance, `win.navigate` action, Ctrl+1…8, F5 reserved, size/route in GSettings); tokens.css + style.css in a GResource bundle; nine placeholder pages; service package skeletons; `apply/registry.py` (SurfaceProvider protocol + registry, no providers); `util/threads.py`; `linwp` CLI stub (exit 3 + milestone); `tools/gtk4_lint.py`; import-linter contracts; pytest unit + smoke tests; fake-root skeleton; `run.sh`, `Makefile` (`make check`), `pyproject.toml`, `CLAUDE.md`, `.claudeignore`. `make check` passes. Not done: `.claude/` universal scaffold (M0.1) — must be deployed by the user. |
| 2026-09-18T17:56:01-04:00 | README: §5 rewritten as the full technology stack with versions and per-milestone status; §12 replaced with the real folder tree; new §11a engineering-practices audit. Fixes from that audit: `Gtk.Window.get_size()` moved behind `compat.window_size()` and `get_size`/`get_children`/`set_position` added to `gtk4_lint` bans; worker pool shut down in `do_shutdown`; `run.sh` tolerates a missing `glib-compile-resources`; `resources/icons/.gitkeep`. |
| 2026-09-18T18:01:57-04:00 | Spec: user exclusions (folder, file, pattern, switchable groups; built-in groups replace the hard-coded list; non-destructive, instant, attributed) — concept §5.1, milestones M1.1a; display detection at scan time (GDK / DRM sysfs / xrandr / declared, physical pixels, `DISPLAY_NOT_DETECTED`) — §5.2, M1.1b; the dimension-based **Ideal for this desktop** segment with per-display sub-segments, near misses and pins — §6.1, M1.5a; schema tables `exclusion`, `exclusion_group`, `display`, `scan`, `ideal_image`, `ideal_pin`; README §2/§4/§10/§13; `linwp` stubs `exclude`, `displays`, `list --ideal/--near-miss`, `scan --display`. |
| 2026-09-18T18:11:03-04:00 | P1 shell aligned to the mockups (`docs/mockups/Sidebar.dc.html`, `Main.dc.html`): Ubuntu family everywhere (`* { font-family: Ubuntu, … }`, `.mono` = Ubuntu Mono, `.tabular` = tnum; `Fonts` in `config_theme.py`; concept §16 and mockups README sentence); sidebar geometry to the pixel (18 px section gap, 28 px nav rows 2 px apart, 0/9 px padding, 12.5 px labels, 14 px icons, logo tile radius 14 with accent→accent-2 gradient and the screen-with-landscape glyph); route glyphs drawn with Cairo in `ui/components/nav_glyph.py` from the mockup paths (theme-independent, colored by the button state via new `compat.foreground_color()`; `NAV_ITEMS` icon names stay as fallback); honest "THIS MACHINE" panel ("Detected in M3", no fake data); page header 22 px/800 title, 12.5 px subtitle, 20/24 px content margins, 14 px gap; cards 16 px padding, 999 px chips 10.5 px/800. New `compat.tracked()` works around GTK 3 measuring labels with letter-spacing of 1 px or more as wrapped. `drive_app.py` takes an optional route argument and screenshots at 1280×800. `make check` passes. |
| 2026-09-18T18:40:41-04:00 | Orchestration: `docs/orchestration.md` (team, rules of engagement, phase gate, plan P0–P7, gate log); `.claude/agents/` deployed with the universal agents plus a new `adversary`; `docs/design/m1-architecture.md` (M1 module contract) with team-lead rulings Q1–Q10a and T1 (crop-loss default 16 %, near miss = linear 90 % coverage, xrandr-first in a session, 64 KiB minimum, Pillow bomb guard left on, `util/threads.py` no longer imports `gi` — GLib scheduler moved to `util/glib_loop.py`). |
| 2026-09-18T18:40:41-04:00 | P1 adversary worklist fixed: Ctrl+Q now persists window state (smoke-tested for close and quit on an isolated keyfile backend); smoke facts assert font, sidebar geometry, nav labels, active pill, footer honesty, page headings, accessible names, hairline; fonts generated from `config_theme.Fonts` only; tokens-only tests reject named colours and numeric Cairo colour literals; `gtk4_lint` catches ~30 more GTK 3-only forms and applies widget-method bans only to widget-capable modules; theme applicator ignores a stale GResource bundle; `make smoke` fails without a display instead of skipping; sidebar hairline drawn over the surface. User request: logo tile, name and tagline centred in the sidebar (deliberate deviation from the mockup, asserted by test). About page heading is now its title. |
| 2026-09-18T18:40:41-04:00 | M1 scaffold: `util/cancel.py`, `capability/reasons.py`, `config_paths.catalogue_db()/thumbs_dir()`, `tests/helpers/imagegen.py`; import contracts "scanner never imports catalogue" and "view models are gi-free except gdk_displays". M1.1b display detection: `scanner/displays.py` (DRM sysfs, xrandr parser and probe, declared displays, Q4 chain, Q5 merge, `DISPLAY_NOT_DETECTED`), 122 tests, 100 % branch coverage. |
| 2026-09-18T18:55:33-04:00 | M1.1a exclusions engine `scanner/exclude.py` (folder/file/pattern rules, switchable groups, seven builtin groups as data, Q8 includes, inode and volume-relative rules, match-everything refusal, `explain()`), M1.4 catalogue core `catalogue/{db,queries,model,schema.sql,migrations}` and M1.5a ideal-images segment `catalogue/ideal.py` (integer arithmetic, Python ≡ SQL on 80k cases, pins, per-display sub-segments). Full gate green: 1030 unit + 19 smoke. Rulings Q2a, P3-1, P4-1…3 added to the M1 contract. |
| 2026-09-19T14:30:00-04:00 | Gate fix + P3/P4 closed: line-wrapped an E501 in `scanner/roots.py` (full `make check` re-verified green); adversary review of P3/P4 returned **PASS WITH FIXES** (0 S1/S2 — Python≡SQL ideal scoring 0 mismatches over 108k cases, ideal rebuild 20k×2 in 53 ms with no file I/O, locked-DB `CatalogueBusyError` at ~5 s and `kill -9`→`interrupted` recovery all reproduced); 4 S3 mutation gaps deferred to P4.1. Planning round: code-reviewer + architect reports folded into the board; rulings Q11–Q17 and R1–R4 added to the M1 contract; `docs/orchestration.md` board expanded to P4.1 / P5a (3 lanes) / P5b / P6 (I→J). |
| 2026-09-19T16:45:00-04:00 | P4.1 + P5a landed (4 parallel implementer lanes, disjoint ownership). P4.1: 3 regression tests closing the every-display `EXISTS` guard, multi-segment literal case-sensitivity, and the `run_migrations` too-new guard. P5a-E: `scanner/roots.py` R1 bug fixed (`/run/media/*/*` now requires 4 segments), R2–R4 addressed (R4 as an additive `propose_all(on_error=…)` hook), new `test_roots.py` (83 tests, 100 %). P5a-F: `scanner/{walker,probe}.py` per §2.7 + rulings Q10a/Q11/Q12/Q15/Q16 (`ProbeStatus.TOO_LARGE`, 64 KiB min, draft-decode thumbnails, whole-catalogue inode index; fixed a sentinel-drop-on-cancel bug found in test), 77 tests. P5a-G: `catalogue/thumbs.py` (sharded 0700 cache, atomic writes, LRU evict, structural `ThumbSink`), `test_thumbs.py` (42 tests, 100 %). Integrated gate green: 1236 unit + 19 smoke, mypy 63 files, 5 import contracts kept, gtk4_lint clean. Adversary re-check of P5a in progress. |
| 2026-09-19T17:30:00-04:00 | P5a closed — adversary **PASS**. Found and fixed one S1: `probe_file` raised on hostile input (Pillow dispatches by magic bytes, so corrupt PPM/SGI/IM content under an allowed extension escaped the `except OSError`); added a catch-all after `Image.open()`/`image.load()` so it returns a `ProbeResult` for any input, plus foreign-magic-bytes fixtures. Team-lead ruling on `max_seconds` (D12): the seconds budget no longer marks a slow-but-valid decode as ERROR — it only gates the optional thumbnail step. Closed 2 S3 mutation gaps (probe `max_seconds` coverage, thumbs 90 % eviction target). Final gate green: 1244 unit + 19 smoke; adversary 6500-case fuzz shows zero residual raise paths, 20/22 mutations caught, no new S1/S2. P5a (roots + walker + probe + thumbs) and P4.1 complete; next is P5b (ingest writer thread + `ScanService` + CLI wiring). |
| 2026-09-20T10:15:00-04:00 | Launch icon + desktop integration + installer. Added the hicolor application icon `resources/icons/hicolor/scalable/apps/io.mensuramedia.LinWallpapers.svg` (full-colour, reproducing `sidebar.py::LogoMark` — the yellow accent→accent-2 tile with the screen-with-landscape mark; colours mirror `tokens.css`) and a monochrome `…-symbolic.svg`; the freedesktop entry `data/io.mensuramedia.LinWallpapers.desktop` (menu name/icon, `StartupWMClass`, Settings/DesktopSettings categories — validates clean); and `install.sh`, a desktop-integration installer (`--user`/`--system`/`--prefix`/`--exec`/`--uninstall`; installs the scalable icon + rasterises PNGs at 16–256 px when a rasteriser exists; rewrites `Exec` to the from-source `run.sh` by default; refreshes icon/desktop caches). shellcheck-clean; `.gitignore` now ignores `.import_linter_cache/`. **No system tray and no autostart** — the project runs no background process, so the symbolic icon is provided for completeness only; the tray/daemon question is recorded as out of scope. Full-distribution packaging remains a later milestone. Handoff notes added under `.claude/memory/`. Local backup taken; committed and pushed. |
| 2026-09-20T13:00:00-04:00 | P5b closed — the app now scans and remembers. `catalogue/ingest.py` (`CatalogueWriter` single writer thread + `ScanService` scan pipeline per concept §2.9/§3: scan_lock → volume heal-by-UUID → detect → snapshot → ideal.rebuild on display change → matcher → per-root walker → bounded queues → scan-private probe pool → batched upsert + ideal.rebuild(ids) + seen_scan → missing-marking after a complete walk → thumbs.evict) and the CLI (`linwp scan/list/show/exclude/displays`, `--json`, exit codes, SIGINT→130). A real scan catalogued the 43-image `~/Pictures/Wallpapers` library to `~/.local/share/lin-wallpapers/catalogue.db`; idempotent rescan re-probes 0. Adversary FAIL→PASS: fixed **S1** (scan of >512 files hung — walker end-of-walk sentinel dropped on a full queue; now delivered via a blocking cancel-polling put in `walker._finish`) and **S2** (thumbnail cache shared one `<key>.jpg.tmp` name across writers → permanent `thumb_status='failed'`; now a unique `tempfile.mkstemp` per writer with lost-race-as-success), plus the P5b thumbs `_ensure_private_dir` mkdir TOCTOU; closed 6 S3 mutation gaps and made the CLI suite display-probe-deterministic. Team-lead accepted 3 minor deviations (ChangeFeed emits post-commit on the orchestrator thread; `scan --add` scans all enabled roots; SIGINT unit test). Gate green: 1272 unit + 19 smoke, mypy 70 files, 5 contracts. Next: P6 (view models → Sources/Browse pages with the Scan button, first-run scan-at-open, results grid). |
| 2026-09-20T18:30:00-04:00 | P6-J landed — the app is clickable. Composition root (`main.py`): builds one `AppServices` with `[GdkDisplayProbe(), *service_probes(/, os.environ)]` + `glib_scheduler`, constructs `ScanVM`/`SourcesVM`/`BrowseVM` once and injects them into the pages through a duck-typed `PageContext` (pages import no view-model/catalogue type — the "widgets never reach past the view models" contract forbids even the indirect edge, so the context carries the browse enum classes too); F5 wired to `ScanVM` rescan and enabled; `do_shutdown` cancels any scan, drops the Browse feed subscription and `services.close()`s (writer + pool joined — nothing left running). `content_area.py` pulls the context off the running application and builds pages via `pages.make_page`. Real **Sources page** (no placeholder): first-run "Find wallpapers" hero listing the real proposed roots with toggles + a prominent Scan button (scans nothing until pressed — acceptance #1), live `scan_banner`, `displays_strip` (real detected screen, else `DISPLAY_NOT_DETECTED` text), `exclusions_panel`. Real **Browse page**: All/Ideal/Near segment switch with live counts, `filter_bar` (search/orientation/aspect/size), and `image_grid` — a `Gtk.FlowBox` in a `ScrolledWindow`, sliding window ≤600 cards, thumbnails delivered as bytes via `BrowseVM.load_thumb` (score-ring slot reserved for M2); real empty states. Scan-at-open (acceptance #4): first run lands on Browse when the catalogue holds images else the Sources hero (tracked by `first_run_done`); a remembered route wins on every later run. New reusable components in `ui/components/{image_grid,image_card,filter_bar,scan_banner,exclusions_panel,displays_strip}.py`; `compat.image_from_bytes`/`clear_children`/`children` added (the one place bytes→pixbuf/texture and child iteration branch on the GTK version). Smoke extended: `drive_scan.py` + `test_browse_fills.py` scan a generated root through the real `ScanVM` and assert the grid fills (10 cards) and an orientation filter narrows it (→6), no GTK criticals, no worker thread surviving window close; persistence smoke updated honestly for the first-run Sources landing. `make check` green: 1292 unit + 22 smoke, mypy 81 files, 5 contracts, gtk4_lint clean. Visual proof: populated Browse and Sources screenshots captured from a real scan. |
| 2026-09-20T19:15:00-04:00 | P6 closed — adversary **PASS**. P6-I (gi-free view models) landed first; en route the integrated gate caught a stale `tools/gtk4_lint.py` assumption (it treated the whole `viewmodels` dir as a widget layer, flagging a plain `set.add()` in the gi-free `BrowseVM`). Team-lead refinement, recorded: gi-free view models are a service layer for the lint — only `gdk_displays.py` (which imports the GTK gate) stays widget-checked; the lint still catches every GTK-touching module, and its test was updated to match. Adversary on the full P6 slice: UI solid (consent real — no scan until the Scan button; layering KEPT; no filesystem in widgets; tokens clean; clean shutdown on normal/cancel close), but found an **S1-class deadlock in the shared `ingest._put`** — a mid-scan *writer-job* failure (disk full, a concurrent `linwp` writer going busy, an sqlite error) ignored the cancel token and hung uncancellably, blocking app shutdown (violates the no-daemon rule). Fixed (lane H): a per-scan `_Abort` signal distinct from user cancel; `_put`/`_probe_worker` poll cancel+abort and never block forever; a writer failure triggers clean teardown recorded as `result='error'` (not `cancelled`) with the class name in the scan row; `cmd_scan` maps `error`→exit 1 (§2.9). Closed 6 VM S3 mutation gaps (lane I: selection ops, last-wins guard, `on_page_shown` data_version, `load_thumb` placeholder reason, 10 Hz throttle, root mutators). Adversary re-check **PASS** across 3 failure triggers, no thread leak, happy path + user-cancel intact, no new S1/S2. Final gate green: 1301 unit + 22 smoke, mypy 81 files, 5 contracts, gtk4_lint clean. **M1 functional core complete: scan → catalogue → ideal-scoring → GUI (Scan button, scan-at-open, results grid) + CLI parity.** Remaining in M1: P7 (perf budgets, 10k fixture, kill-9/cancel acceptance run, `docs/perf.md`). |
| 2026-09-20T20:30:00-04:00 | Browse right-click **Exclude Image / Exclude Folder** (first slice of the context-menu feature spec'd in `docs/design/browse-context-menu.md`). Right-clicking a card opens a `Gtk.PopoverMenu` (Exclude Image · Exclude Folder · ─ · Add to Collection [disabled "Arrives in M6"] · Preview… [disabled "Arrives in M3"] — never-hide-a-feature). `BrowseVM.exclude_image/exclude_folder/undo_exclusion` (gi-free): a FILE rule is **inode-pinned** (survives a rename) and a FOLDER rule on the dir; the excluded card leaves Browse via the ChangeFeed→refresh and appears in Sources → Exclusions; an **Undo toast** (auto-dismiss 6 s, cancelled on replace/undo/unrealize) restores it. Non-destructive — only an `exclusion` row + `image.excluded_by` change; no file touched (adversary verified files intact with identical inodes after exclude+undo). New `ui/components/{context_menu,toast}.py`; `compat.secondary_click_gesture`/`menu_popover` (GTK-version branching kept in compat). Adversary PASS (no S1/S2); closed 3 S3 gaps (inode/rename survival, feed-driven refresh, the real menu/toast smoke path), each mutation-verified. Gate green: 1305 unit + 24 smoke, mypy 83 files, 5 contracts, gtk4_lint clean. |
