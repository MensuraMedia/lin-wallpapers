# Runtime dependencies come from the distribution, not PyPI (README §5):
#   sudo apt install python3-gi gir1.2-gtk-3.0 gir1.2-gdkpixbuf-2.0 python3-gi-cairo python3-pil
# run.sh creates the venv with --system-site-packages so those are visible inside it.
